<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.backtoschool.testself"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="16" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="แบบทดสอบค้นหาตัวเอง"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.backtoschool.testself.MainActivity"
            android:theme="@style/splashScreenTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu"
            android:theme="@style/splashScreenTheme"/>
        <activity android:name=".PersonalActivity"
            android:theme="@style/splashScreenTheme"/>
        <activity android:name=".ResultActivity"
            android:theme="@style/splashScreenTheme"/>
        <activity android:name=".TestMenuActivity"
            android:theme="@style/splashScreenTheme"/>
        <activity android:name=".ArtTestActivity"
            android:theme="@style/splashScreenTheme"/>
        <activity android:name=".ScienceTestActivity"
            android:theme="@style/splashScreenTheme"/>
    </application>

</manifest>
